# Copy this file to .env and fill in the value.
# .env is gitignored — .env.example is committed.

# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Postgres — see docker-compose.yml for the local dev values
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5433/repolens

